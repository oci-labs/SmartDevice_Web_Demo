model {
    Department department
}

json {
    id department.id
    name department.name
    type 'department'
    facility tmpl.facility(department.facility)
    machines tmpl.machine("_machine", department.machines)
}