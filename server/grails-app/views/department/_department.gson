import com.nexmatix.Department

model {
    Department department
}

json {
    id department.id
    name department.name
    type 'department'
    parent tmpl.'/templates/facility'(department.facility)
    children tmpl.'/templates/machine'("machine", department.machines)
}