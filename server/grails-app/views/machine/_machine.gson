model {
    Machine machine
}

json {
    id machine.id
    name machine.name
    type 'machine'
    department tmpl.department(machine.department)
    manifolds tmpl.manifold("manifold", machine.manifolds)
}