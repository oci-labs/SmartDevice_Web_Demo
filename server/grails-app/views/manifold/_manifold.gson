model {
    Manifold manifold
}

json {
    id manifold.id
    name manifold.name
    type 'manifold'
    machine tmpl.machine(manifold.machine)
    stations tmpl.station("station", manifold.stations)
}