import com.nexmatix.Department

model {
    Department department
}

json {
    id department.id
    name department.name
    type 'department'
    parent department.facility.id
    children tmpl.machine("machine", department.machines)
}